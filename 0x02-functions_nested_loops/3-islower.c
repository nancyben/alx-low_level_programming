void _putchar(char);

/**
 * _islower - checks if a character is lowecased or not
 *
 * @c: character to check
 *
 * Return: returns 0 if lowercased otherwise return 1
 */
int _islower(char c)
{
	if (c >= 97)
		return (1);
	else
		return (0);
}
