#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char txt[] = "with proper grammar%c but the outcome is a piece of art%c\n";

	printf(txt, ',', ',');
	return (0);
}
